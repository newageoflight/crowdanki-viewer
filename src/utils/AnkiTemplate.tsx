// renders Anki templates using Javascript
import { NoteInterface } from './../interfaces/NoteInterface';
import { CardTemplateInterface } from './../interfaces/CardTemplate';

// cloze
export function renderClozeTemplate(note: NoteInterface, template: CardTemplateInterface) {
    return "";
}

// everything else
export function renderTemplate(note: NoteInterface, template: CardTemplateInterface) {
    return "";
}